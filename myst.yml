# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  id: 1edcd483-ccf0-4cfe-9331-59f4145a1504
  # title:
  # description:
  # keywords: []
  # authors: []
  # github:
  # To autogenerate a Table of Contents, run "myst init --write-toc"
  error_rules:
    - rule : valid-page-frontmatter
      severity: ignore
site:
  template: book-theme
  options:
    logo_text: PRBEM Documents
  # options:
  #   favicon: favicon.ico
  #   logo: site_logo.png
